# VLABench select_book 首次 rollout 诊断

配置：Wan2.1 1.3B select_book 最终 step002810，原始 VAE，legacy，replan8，夹爪阈值0.5；Track1 episode0，seed42。诊断运行输出位于：

`evaluate_results/vlabench/vlabench_select_book_rothko_full_wan21_1_3b/ckpt002810_vaeOriginal_replan8_legacy_ep1_diagnostics_v2/`

## 已观测到的结果

- 200步，success=false，intention_score=0，progress_score=0。
- 步骤编号从0开始。0–134步的IK成功；首次IK失败在135步。因此38–39步附近开始的执行偏差不是先由IK求解失败引起。
- 第39步：请求位移13.8mm，实际移动1.4mm；执行后位置误差15.1mm、旋转误差2.8°。IK目标姿态误差近浮点零。
- 第47步：执行后位置误差53.8mm，旋转误差7.3°，IK仍成功。
- 第63步：请求目标相对当前EE位移101.3mm，执行后位置误差109.5mm、旋转误差29.1°；IK仍成功。
- 第135步开始出现不可精确求解的目标。此次共22步IK失败：135、139–143、184–199。上一轮日志有14条未收敛警告，不能宣称两次整条轨迹逐bit一致。
- 当前记录中的200个已执行动作，夹爪命令全部为两指0.04，即始终张开。全部200个已执行预测帧的连续opening范围0.998046875–1，不是阈值0.5将一个明显的闭合预测误判为打开。

## 判断与边界

视频显示接近书架后持续推进、书本被碰倒，随后手臂姿态逐渐扭转。结合IK仍可精确求解而实际EE移动很小，更符合接触/控制跟踪受阻后继续执行目标的过程。没有记录接触力，故不能据此精确断言碰撞对象或排除动力学因素。

可排除的是“最早异常由IK未收敛直接触发”；不能排除碰撞无关的IK姿态分支、动力学影响，也不能仅凭曲线平滑判断动作正确。全程没有有效闭合预测，提示策略没有完成正确抓取；这可能是轨迹偏离后一直没进入闭合阶段，不足以单独认定夹爪训练损坏。

当前未发现相机顺序、原点加减、四元数排序的明显接入错误，但未与同场景专家逐步对齐，尚不能断言模型参数是唯一原因。下一步若继续诊断，优先比较同场景专家的接近/抓取路径及夹爪闭合时机，而不是先改IK或碰撞设置。

## 记录说明

`diagnostics_episode_000/steps.jsonl` 同一行包含请求xyz/wxyz、执行前后EE、IK成功标志、IK关节解及其FK位姿、实际关节值、夹爪命令。`replan_*.json` 保存完整预测pose/gripper与该chunk参考位姿。

`diagnostics_episode_000/comparison.md` 是逐步表，图像链接对应 `frames/step_XXXX.png`（动作前观测）。位置误差单位m，旋转为SO(3)测地角（度）；不是Euler分量差。

诊断仅由单入口 `--diagnostics` 开启，默认关闭；通过包装官方调用记录数据，不改动作、不重试IK、不改阈值。IK关节解的FK在独立physics副本计算，不写rollout状态。初次诊断包装器缺少reset无参数step兼容性，动作前即退出；修复后在_v2目录完成。旧无诊断结果保留。
