# @package _global_
defaults:
  - vlabench_rothko_2cam224_full_wan21_1_3b_1e-4
  - override /data: vlabench_rothko_3cam192
  - _self_

output_dir: ./runs/vlabench_select_book_rothko_3cam192_wan21/${now:%Y-%m-%d}_${now:%H-%M-%S}
data:
  train:
    latent_cache_dir: ./data/vlabench_train99_rothko_3cam192_centerfrac05_wan21_bf16_h16_latents/select_book
    latent_cache_only: true
model:
  video_dit_config:
    use_gradient_checkpointing: false
# Same effective batch and training budget as the previous select_book run.
batch_size: 4
gradient_accumulation_steps: 4
num_workers: 4
num_epochs: 10
eval_sample_manifest: ./data/vlabench_primitive_ft_lerobot/manifests/validation_windows_select_book_seed42.json
wandb:
  name: vlabench_select_book_rothko_3cam192_wan21
